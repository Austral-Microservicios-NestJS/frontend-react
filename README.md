## DEV

1. Clonar el repositorio de github
2. Crear un archivo `.env` basado en el `.env.example`
3. Instalar las dependencias con `pnpm install`
4. Ejecutar el proyecto con `pnpm dev`

Si no tienes `pnpm` puedes instalarlo desde la terminal con el siguiente comando

```
npm i -g pnpm
```
